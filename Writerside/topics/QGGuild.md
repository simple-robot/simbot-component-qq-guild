# 频道 QGGuild


## API

API模块中部分用于获取频道信息的API中会返回一些与 `Guild` 相关的数据类型。

当你使用 `GetGuildApi` 或 `GetBotGuildListApi` 时，
返回值类型分别为 `SimpleGuild` 和 `List<SimpleGuild>`。

## 组件库中的QGGuild {id="component-qg-guild"}

TODO
